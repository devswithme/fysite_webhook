version: "3.8"

services:
  app:
    build: .
    container_name: webhook
    restart: always
    env_file:
      - .env
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:8.2-rc1-alpine
    container_name: webhook_cache
    restart: unless-stopped
